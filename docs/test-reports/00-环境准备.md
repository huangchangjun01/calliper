# 测试环境准备记录（Task 1）

执行时间：2026-09-03

## 环境核验

| 组件 | 端口 | 状态 | 备注 |
|---|---|---|---|
| 后端 Gateway | 8080 | ✅ 运行中 | /health → `{"status":"healthy"}` |
| ML 服务 | 8000 | ✅ 运行中 | uvicorn app.main:app；模型健康：short v2.0.7-real(acc 0.50)、medium v2.0.4-real(acc 0.408)、long v2.0.4-real(acc 0.786)，均 is_healthy=true |
| Redis | 6379 | ✅ 运行中 | — |
| PostgreSQL（主库+TSDB 同实例） | 5432 | ✅ 运行中 | calliper_trading 202 只股票；TSDB_PORT=5432（backend/.env） |
| 前端 | 3000 | ✅ 已启动（本测试启动） | vite dev，/api 与 /ws 代理到 8080 |
| Kafka | — | ⏸ no-op | KAFKA_BROKERS 为空 |

## 测试账号（新增，未触碰已有用户）

| 账号 | 角色 | 密码 | 注册方式 | 登录验证 |
|---|---|---|---|---|
| testuser | user | pass1234 | POST /auth/register (201) | ✅ 200，token/role=user |
| testadmin | admin | pass1234 | POST /auth/register (201) + SQL 提升 role=admin | ✅ 200，token/role=admin |

说明：库中已有 `huangchangjun`(admin, 用户自有) 与多个 dashcheck 测试账号，未做任何改动；未创建 spec 原设 `admin/admin123`（登录 401 确认不存在），改用上述测试专用账号。

## 数据前置

- stocks 表：202 条（symbol 为纯数字，如 600000），search/market 用例可直接执行
- ML 模型：三周期均已训练且健康，PRED/EVAL/DASH 用例可基于真实模型执行
- 预测数据：执行 PRED-017 生成用例时落库（供 PRED/EVAL/DASH 消费）